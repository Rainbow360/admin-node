{{each data}}
<tr>
    <td>{{ $index + 1 }}</td>
    <td>{{ $value.username }}</td>
    <td><button data-id="{{ $value._id }}" type="button" class="btn btn-danger btn-primary remove">删除</button></td>
</tr>
{{/each data}}